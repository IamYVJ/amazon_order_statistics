# Selenium 4.6+ ships Selenium Manager, which downloads a matching
# geckodriver/chromedriver automatically - no driver setup needed.
selenium>=4.16.0

# Only used as a fallback when USE_DRIVER_MANAGER = True.
# 3.x cannot resolve Chrome for Testing drivers (Chrome 115+), so 4.0.1+ is required.
webdriver-manager>=4.0.1
